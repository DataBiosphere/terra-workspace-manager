<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender" immediateFlush="false">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%-5level] %logger{15} - %msg%n%rEx</pattern>
        </encoder>
    </appender>

    <!-- uncomment and set to DEBUG to log all failing HTTP requests -->
    <!-- uncomment and set to TRACE to log all HTTP requests -->
    <logger name="io.gatling.http.engine.response" level="TRACE"/>

    <!-- uncomment for logging around wblibs google API calls -->
    <!-- <logger name="org.broadinstitute.dsde.workbench.google" level="DEBUG" />-->

    <!-- useful information about service account creation -->
    <!-- <logger name="simulations.TerraUserHelper" level="INFO" />-->

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>