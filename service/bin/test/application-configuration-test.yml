# This configuration is specifically used to test WSM reading application configuration files.
# These are not real applications. If you need a real application for connected tests, use
# the app-test profile instead (see application-app-test.yml).
workspace:
  application:
    configurations:
      SanDiego:
        name: Sunny
        description: beachgoing framework
        service-account: sandiego@terra-dev.iam.gserviceaccount.com
        state: operating
      Carmen:
        name: Carmen
        description: musical performance framework
        service-account: carmen@terra-dev.iam.gserviceaccount.com
        state: deprecated
