apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-director-deployment
  labels:
    jmeter_mode: {{ .Chart.Name }}-director
spec:
  replicas: 1
  selector:
    matchLabels:
      jmeter_mode: {{ .Chart.Name }}-director
  template:
    metadata:
      labels:
        jmeter_mode: {{ .Chart.Name }}-director
    spec:
      serviceAccountName: {{ .Chart.Name }}-serviceaccount
      containers:
        - name: {{ .Chart.Name }}-director
          image: {{ .Values.directorImage }}
          imagePullPolicy: {{ .Values.imageConfig.imagePullPolicy }}
          command: [ "/bin/bash", "-c", "--" ]
          args: [ "while true; do sleep 30; done;" ]
          volumeMounts:
            - name: loadtest
              mountPath: /load_test
              subPath: "load_test"
          ports:
            - containerPort: 60000
          env:
            - name: FIRECLOUD_SERVICE_ACCOUNT_CREDS
              valueFrom:
                secretKeyRef:
                  name: {{ .Chart.Name }}-director-worker-secretdefinition
                  key: firecloud-account.json
            - name: SAM_USER
              valueFrom:
                configMapKeyRef:
                  name: {{ .Chart.Name }}-{{ .Values.targetEnv }}-configmap
                  key: sam_user
            - name: WSM_PROTOCOL
              valueFrom:
                configMapKeyRef:
                  name: {{ .Chart.Name }}-{{ .Values.targetEnv }}-configmap
                  key: wsm_protocol
            - name: WSM_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ .Chart.Name }}-{{ .Values.targetEnv }}-configmap
                  key: wsm_port
            - name: WSM_SERVER
              valueFrom:
                configMapKeyRef:
                  name: {{ .Chart.Name }}-{{ .Values.targetEnv }}-configmap
                  key: wsm_server
            - name: INFLUXDB_WSM_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ .Chart.Name }}-{{ .Values.targetEnv }}-configmap
                  key: influxdb_wsm_url
            - name: INFLUXDB_WSM_MEASUREMENT
              valueFrom:
                configMapKeyRef:
                  name: {{ .Chart.Name }}-{{ .Values.targetEnv }}-configmap
                  key: influxdb_wsm_measurement
      volumes:
        - name: loadtest
          configMap:
            name: {{ .Chart.Name }}-director-configmap